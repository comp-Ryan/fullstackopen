{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "Ryan Lee",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "build:ui": "shx rm -rf dist && cd ../Part2/Phonebook && npm run build && shx cp -r dist ../../Part3",
    "deploy:full": "npm run build:ui && git add . && git commit && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^9.3.0",
    "morgan": "^1.10.1",
    "shx": "^0.4.0"
  }
}
